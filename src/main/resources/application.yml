server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: ROOT
    url: jdbc:postgresql://localhost:5432/city_card_db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true